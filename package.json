{
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.12.0",
    "concurrently": "^9.2.4",
    "playwright": "^1.62.1"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -k -n server,web -c blue,green \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "typecheck": "npm --prefix server run typecheck && npm --prefix web run typecheck",
    "test": "npm --prefix server test && npm --prefix web test",
    "sandcastle": "npx tsx .sandcastle/main.mts",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  }
}
